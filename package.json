{
  "name": "getting-started-typescript",
  "version": "1.0.0",
  "description": "Bolt getting started app in TypeScript",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -w -p .",
    "start": "npm run build && node dist/app.js",
    "release-it": "npm run release-it"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.3.0",
    "body-parser": "^1.20.0",
    "dotenv": "^8.2.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "ts-node": "^9.1.1",
    "release-it": "^14.14.2",
    "typescript": "^4.2.3"
  }
}
